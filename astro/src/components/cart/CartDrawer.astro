---
export interface Props {
  restaurantId: number;
  whatsappPhone: string;
}

const { restaurantId, whatsappPhone } = Astro.props;
---

<div id="cart-overlay" class="fixed inset-0 z-30 hidden bg-black/60 backdrop-blur-sm"></div>

<div
  id="cart-sidebar"
  class="fixed right-0 top-0 z-40 flex h-full w-full max-w-sm translate-x-full transform flex-col bg-[#0f0f0f] shadow-2xl transition-transform duration-300"
  data-restaurant-id={restaurantId}
  data-whatsapp={whatsappPhone}
>
  <div class="flex items-center justify-between border-b border-white/5 p-4">
    <h2 class="text-lg font-bold text-[#f5f5f7]">Tu pedido</h2>
    <button id="cart-close" class="rounded-full p-2 text-[#a1a1aa] transition hover:bg-white/10">
      <svg class="h-5 w-5" fill="none" viewBox="0 0 24 24" stroke="currentColor">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
      </svg>
    </button>
  </div>
  <div id="cart-items" class="flex-1 overflow-y-auto p-4">
    <p class="py-10 text-center text-sm text-[#71717a]">Tu pedido está vacío</p>
  </div>
  <div id="cart-footer" class="hidden border-t border-white/5 p-4">
    <div class="mb-3 flex items-center justify-between text-sm">
      <span class="text-[#a1a1aa]">Total</span>
      <span id="cart-total" class="text-lg font-bold text-brand-400">$0.00</span>
    </div>
    <button
      id="clear-cart-btn"
      class="mb-2 w-full rounded-xl border border-white/10 py-2 text-xs font-medium text-[#a1a1aa] transition hover:bg-white/5"
    >
      Vaciar pedido
    </button>
    <button
      id="checkout-btn"
      class="w-full rounded-xl bg-brand-600 py-3 text-sm font-semibold text-white shadow-lg transition hover:bg-brand-500"
    >
      Comprar ahora
    </button>
  </div>
</div>

<script>
  const sidebar = document.getElementById('cart-sidebar');
  const restaurantIdNum = parseInt(sidebar?.getAttribute('data-restaurant-id') || '0');
  const whatsapp = sidebar?.getAttribute('data-whatsapp') || '';

  function loadCartItems() {
    const raw = localStorage.getItem(`velziaCart_${restaurantIdNum}`);
    if (!raw) return [];
    try {
      const parsed = JSON.parse(raw);
      return parsed.items || [];
    } catch {
      return [];
    }
  }

  function saveCartItems(items: any[]) {
    localStorage.setItem(`velziaCart_${restaurantIdNum}`, JSON.stringify({ timestamp: Date.now(), items }));
    document.dispatchEvent(new CustomEvent('cart-updated'));
  }

  function openCart() {
    document.getElementById('cart-sidebar')?.classList.remove('translate-x-full');
    document.getElementById('cart-overlay')?.classList.remove('hidden');
    document.getElementById('product-detail')?.classList.add('translate-x-full');
    renderCart();
  }

  function closeCart() {
    document.getElementById('cart-sidebar')?.classList.add('translate-x-full');
    document.getElementById('cart-overlay')?.classList.add('hidden');
  }

  function renderCart() {
    const container = document.getElementById('cart-items');
    const footer = document.getElementById('cart-footer');
    const totalEl = document.getElementById('cart-total');
    if (!container) return;

    const items = loadCartItems();
    if (items.length === 0) {
      container.innerHTML = '<p class="py-10 text-center text-sm text-[#71717a]">Tu pedido está vacío</p>';
      footer?.classList.add('hidden');
      return;
    }

    container.innerHTML = items.map((item: any, i: number) => {
      const extras = (item.selectedModifiers || []).reduce((s: number, m: any) => s + (m.extra_price || 0), 0);
      const unit = item.product.price + extras;
      return `
        <div class="mb-3 rounded-xl border border-white/5 bg-[#141414] p-3" data-index="${i}">
          <div class="flex items-start justify-between">
            <div class="flex-1 pr-2">
              <h4 class="text-sm font-semibold text-[#f5f5f7]">${item.product.name}</h4>
              ${item.selectedModifiers?.length ? `<p class="mt-1 text-xs text-[#71717a]">${item.selectedModifiers.map((m: any) => m.name).join(', ')}</p>` : ''}
              <p class="mt-1 text-xs text-brand-400 font-bold">$${unit.toLocaleString('es-CO')} c/u</p>
            </div>
            <button class="remove-item flex h-6 w-6 shrink-0 items-center justify-center rounded-full text-[#71717a] transition hover:bg-red-500/20 hover:text-red-400" data-index="${i}" title="Eliminar">
              <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18M8 6V4a2 2 0 0 1 2-2h4a2 2 0 0 1 2 2v2m3 0v14a2 2 0 0 1-2 2H7a2 2 0 0 1-2-2V6"/></svg>
            </button>
          </div>
          <div class="mt-3 flex items-center justify-between">
            <div class="flex items-center gap-3 rounded-full border border-white/10 px-2 py-1">
              <button class="qty-minus flex h-6 w-6 items-center justify-center text-[#f5f5f7] transition hover:text-brand-400" data-index="${i}">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="5" y1="12" x2="19" y2="12"/></svg>
              </button>
              <span class="min-w-5 text-center text-sm font-semibold text-[#f5f5f7]">${item.quantity}</span>
              <button class="qty-plus flex h-6 w-6 items-center justify-center text-[#f5f5f7] transition hover:text-brand-400" data-index="${i}">
                <svg xmlns="http://www.w3.org/2000/svg" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg>
              </button>
            </div>
            <span class="text-sm font-bold text-brand-400">$${(unit * item.quantity).toLocaleString('es-CO')}</span>
          </div>
        </div>
      `;
    }).join('');

    const total = items.reduce((s: number, item: any) => {
      const extras = (item.selectedModifiers || []).reduce((m: number, mod: any) => m + (mod.extra_price || 0), 0) || 0;
      return s + (item.product.price + extras) * item.quantity;
    }, 0);

    if (totalEl) totalEl.textContent = '$' + total.toLocaleString('es-CO');
    footer?.classList.remove('hidden');
  }

  document.getElementById('cart-toggle-btn')?.addEventListener('click', openCart);
  document.getElementById('cart-close')?.addEventListener('click', closeCart);
  document.getElementById('cart-overlay')?.addEventListener('click', closeCart);

  document.getElementById('clear-cart-btn')?.addEventListener('click', () => {
    localStorage.removeItem(`velziaCart_${restaurantIdNum}`);
    document.dispatchEvent(new CustomEvent('cart-updated'));
    renderCart();
    closeCart();
  });

  document.getElementById('cart-items')?.addEventListener('click', (e) => {
    const target = e.target as HTMLElement;
    const minusBtn = target.closest('.qty-minus') as HTMLElement;
    const plusBtn = target.closest('.qty-plus') as HTMLElement;
    const removeBtn = target.closest('.remove-item') as HTMLElement;

    if (minusBtn) {
      const idx = parseInt(minusBtn.getAttribute('data-index') || '0');
      const items = loadCartItems();
      if (items[idx]) {
        items[idx].quantity -= 1;
        if (items[idx].quantity <= 0) items.splice(idx, 1);
        saveCartItems(items);
        renderCart();
        if (items.length === 0) closeCart();
      }
    } else if (plusBtn) {
      const idx = parseInt(plusBtn.getAttribute('data-index') || '0');
      const items = loadCartItems();
      if (items[idx] && items[idx].quantity < 99) {
        items[idx].quantity += 1;
        saveCartItems(items);
        renderCart();
      }
    } else if (removeBtn) {
      const idx = parseInt(removeBtn.getAttribute('data-index') || '0');
      const items = loadCartItems();
      items.splice(idx, 1);
      saveCartItems(items);
      renderCart();
      if (items.length === 0) closeCart();
    }
  });

  document.getElementById('checkout-btn')?.addEventListener('click', () => {
    closeCart();
    const event = new CustomEvent('open-checkout');
    document.dispatchEvent(event);
  });
</script>
